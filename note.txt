

further
吃饭
买水
买厚笔记本（蛋疼）

下载器模式

目前实现的下载器对象非常难用而且不灵活。我们试图建立更一般的模式

下载器接收一个记录对象，这个对象同时记录数据和状态。其应该是一个树。每个节点对应一个下载，下载器按照某种规则（可以封装为有限多个方法）遍历。

当一个节点完不成时（出于各种奇葩原因），应该允许指定无视这个错误继续跑下去，同时对这个节点做一个特殊的标记，以备之后特别处理。

这尼玛数据不一致性太严重了，还怎么好好玩耍。